version: '3.8'

services:
  app:
    build: .
    container_name: simple-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./app:/app
    restart: unless-stopped